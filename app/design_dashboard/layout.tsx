// shared elements for glass bulder: bottom form buttons 

export default function Layout({ children }: {children: React.ReactNode}){
    return <p>Layout for glass dashboard: need back, finish later, and next buttons</p>
}