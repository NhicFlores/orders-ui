import LoginForm from '@/components/auth/LoginForm'
//keep page as a server component
// tag components as client components so they can manage state

const LoginPage = () => {
  return (
    <LoginForm/>
  )
}

export default LoginPage
